# NATS jnats library native-image configuration
# These classes must be initialized at runtime to prevent fixed seeds in randomization
Args = --initialize-at-run-time=io.nats.client.support.RandomUtils,io.nats.client.NUID
